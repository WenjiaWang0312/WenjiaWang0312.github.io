# Migration TODO

## Done
- Initialized a clean migration repo from `academicpages`
- Replaced template identity with Wenjia Wang's basic profile
- Migrated the homepage intro and a selected news list
- Added a `Projects` landing page
- Copied the legacy `projects/` directory into this repo
- Replaced sample `CV` and `Publications` pages with real placeholders

## Next
- Convert legacy publications into `_publications/*.md`
- Decide whether to keep legacy project pages as-is or gradually refactor them into `_portfolio/`
- Add a cleaner home/news layout if needed
- Move downloadable files and PDFs into `files/`
- Update `_config.yml` `url` and `repository` after the new remote is created
